name = "bitter-violet-3f1a"
type = "javascript"
compatibility_date = "2024-01-01"
main = "worker/worker.js"
# Secrets (VPN_MANAGER_URL, VPN_MANAGER_SECRET) are set via `wrangler secret put`
# and automatically available as globals in the Worker
